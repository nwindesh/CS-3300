<p id="notice"><%= notice %></p>

<h1 style="text-align:center">Projects</h1>

<p style = "text-align:center">This is Nic's project portfolio. The projects below are the projects I have completed and added into my portfolio.
Each contains a short description with more detail about the project.</p>

<table class="table table-striped table-secondary">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr class="">
        <td><%= project.title %></td>
        <td><%= project.description %></td>
        <td><%= link_to 'Show', project %></td>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
    <tr>
        <td><%= link_to 'New Project', new_project_path %></td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
    </tr>
    
  </tbody>
</table>

<br>

